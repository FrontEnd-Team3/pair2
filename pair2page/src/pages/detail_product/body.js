import styled from 'styled-components'

const Body = () => {
	return (
		<>
			<ul>{}</ul>
			<Box>The page for body products.</Box>
		</>
	)
}

export default Body

const Box = styled.div`
	height: 800px;
`
