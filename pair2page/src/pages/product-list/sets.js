import styled from 'styled-components'

const Sets = () => {
	return (
		<>
			<ul>{}</ul>
			<Box>The page for sets products.</Box>
		</>
	)
}

export default Sets

const Box = styled.div`
	height: 800px;
`
